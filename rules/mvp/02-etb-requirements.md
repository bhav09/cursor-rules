## Enhanced Task Brief (ETB) – MVP

This segment is REQUIRED before any code is written.

ETB must be written to `project.md` and include:

1) User goal (1 sentence)
2) Mode: [MVP] + reason
3) Files read + files to modify
4) Dependency check
5) MVP safety checklist
6) Step-by-step plan
7) Acceptance criteria
8) Optional rollout plan

After ETB:
- Wait for user approval
- THEN load `03-repo-grounding.md`
